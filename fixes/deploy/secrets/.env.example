# ⚠ This file is intentionally a POINTER, not a second source of truth (diagnostic #6).
# The single canonical template is the repository-root  .env.example  (all current variable names).
# Copy THAT to .env:   cp .env.example .env
# Do NOT reintroduce deprecated names here (MINIO_ACCESS_KEY, POSTGRES_DSN, GLPI_ADAPTER_URL, …).