# =============================================================================
# Telecom AI Voice Agent — environment template.  Copy to .env and fill in.
# ⚠ = must be set for a real (live) deployment.  Unset = safe dev/mock default.
# =============================================================================

# ---- Core datastore (platform DB on 15432 to avoid the LiveKit DB on 5432) ----
DATABASE_URL=postgresql+psycopg://telecom:telecom@localhost:15432/telecom
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30.0
DB_POOL_RECYCLE=1800
# infra compose credentials (⚠ change for prod; referenced by docker-compose)
POSTGRES_USER=telecom
POSTGRES_PASSWORD=telecom            # ⚠ CHANGE
POSTGRES_DB=telecom

# ---- Adapter mode: mock (local Postgres) or live (real OCS/Billing/Payment/GLPI/SMS) ----
CONNECTOR_MODE=mock                  # mock | live

# ---- Internal service-to-service auth (report #17). Unset = disabled (dev). ----
INTERNAL_API_KEY=                    # ⚠ set one strong shared key across all internal services

# ---- Edge CORS allow-lists (report #15). Comma-separated origins. ----
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ---- LiveKit (self-hosted transport) ----
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=devkey               # ⚠ CHANGE
LIVEKIT_API_SECRET=devsecret_change_me   # ⚠ CHANGE

# ---- Voice + LLM providers (agent-worker) ----
DEEPGRAM_API_KEY=                    # ⚠ STT
ELEVEN_API_KEY=                      # ⚠ TTS
ELEVEN_VOICE_ID=EXAVITQu4vr4xnSDxMaL
OPENAI_API_KEY=                      # ⚠ LLM primary (+ embeddings for Qdrant)
GOOGLE_API_KEY=                      # ⚠ LLM fallback (Gemini)
AZURE_SPEECH_KEY=                    # optional (Azure STT/TTS presets)
AZURE_SPEECH_REGION=
STT_MODEL=nova-3
TTS_MODEL=eleven_flash_v2_5
LLM_PRIMARY_MODEL=gpt-4.1-mini
LLM_FALLBACK_MODEL=gemini-2.0-flash

# ---- Language / session ----
SUPPORTED_LANGUAGES=fr,ar,en
DEFAULT_LANGUAGE=fr
SESSION_LANGUAGE=fr
SESSION_CALLER_MSISDN=

# ---- Internal service URLs (agent-worker → services) ----
CONTEXT_SERVICE_URL=http://localhost:8101
KNOWLEDGE_SERVICE_URL=http://localhost:8102
DECISION_SERVICE_URL=http://localhost:8103
POLICY_SERVICE_URL=http://localhost:8104
EXECUTION_SERVICE_URL=http://localhost:8105
NOTIFICATION_SERVICE_URL=http://localhost:8106

# ---- MCP servers ----
KNOWLEDGE_MCP_URL=http://localhost:8201/mcp
TICKETING_MCP_URL=http://localhost:8202/mcp
MESSAGING_MCP_URL=http://localhost:8203/mcp

# ---- GLPI ticketing (live). ⚠ BIND to a real GLPI instance. ----
GLPI_BASE_URL=                       # e.g. https://glpi.telecom.tn/apirest.php
GLPI_APP_TOKEN=                      # ⚠
GLPI_USER_TOKEN=                     # ⚠

# ---- Messaging providers (notification-service, live). ⚠ BIND. ----
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_SMS_FROM=
TWILIO_WHATSAPP_FROM=
SENDGRID_API_KEY=
EMAIL_FROM=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# ---- Domain adapter endpoints (live OCS / Billing / Payment / CRM / NMS). ⚠ BIND. ----
OCS_ADAPTER_URL=
BILLING_ADAPTER_URL=
PAYMENT_ADAPTER_URL=
CRM_ADAPTER_URL=
NMS_ADAPTER_URL=
PROVISIONING_ADAPTER_URL=

# ---- Knowledge / RAG (Qdrant) ----
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=telecom_knowledge
EMBEDDING_MODEL=text-embedding-3-small   # or a sentence-transformers model id

# ---- Cache (Redis). Unset = caching disabled. ----
REDIS_URL=redis://localhost:6379/0
CACHE_TTL_SECONDS=300

# ---- Object storage (MinIO) for call recordings. ----
MINIO_ENDPOINT=localhost:9000
MINIO_ROOT_USER=minioadmin           # ⚠ CHANGE
MINIO_ROOT_PASSWORD=minioadmin       # ⚠ CHANGE
MINIO_BUCKET=call-recordings
MINIO_SECURE=false

# ---- Observability (OpenTelemetry). Unset = telemetry no-op. ----
OTEL_EXPORTER_OTLP_ENDPOINT=         # e.g. http://localhost:4317

# ---- business-api RBAC (dev). Replace with OIDC at integration. ----
BUSINESS_API_DEFAULT_ROLE=administrateur

# ---- Frontends (Vite) ----
VITE_TOKEN_URL=http://localhost:8107
VITE_BUSINESS_API_URL=http://localhost:8108
VITE_API_ROLE=administrateur