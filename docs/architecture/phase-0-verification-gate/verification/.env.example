# Phase-0 verification spike — provider keys (Twelve-Factor; never commit real values).
# Primary providers (required for a real round-trip):
DEEPGRAM_API_KEY=dg_xxx                 # Deepgram Nova-3 STT (FR/EN multi, AR monolingual)
ELEVENLABS_API_KEY=sk_xxx               # ElevenLabs Flash v2.5 TTS (FR/AR/EN)
# Fallback provider (Azure Speech STT + Neural TTS, all three languages):
AZURE_SPEECH_KEY=xxxxxxxx
AZURE_SPEECH_REGION=francecentral
# LLM topology (bound in Phase 1; listed here for completeness):
OPENAI_API_KEY=sk-xxx                   # primary LLM (GPT-4.1 family)
GOOGLE_API_KEY=xxx                      # fallback LLM (Gemini 2.x)
