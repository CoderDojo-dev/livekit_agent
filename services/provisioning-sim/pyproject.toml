[project]
name = "provisioning-sim"
version = "0.1.0"
description = "SIM lifecycle / provisioning simulator (in-memory ledger)"
requires-python = ">=3.12"
dependencies = [
  "service-auth",
  "persistence",
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.34,<1",
]
